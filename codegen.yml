overwrite: true
schema: ${NEXT_PUBLIC_APOLLO_CLIENT_URI}
documents: "graphql/**/*.graphql"
generates:
  graphql/generated/index.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"